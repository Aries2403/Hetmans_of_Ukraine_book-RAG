#### 2. `render.yaml` — у папці `app/`
```yaml
services:
  - type: web
    name: hetman-rag
    env: python
    buildCommand: pip install -r requirements.txt && python rag_hetmans.py
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: HF_HUB_DISABLE_SYMLINKS_WARNING
        value: "1"